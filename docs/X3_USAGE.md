# X3 usage (renamed)

The Xteink e-ink writer is documented in [XTEINK_USAGE.md](XTEINK_USAGE.md).

`--x3` remains a compatibility alias for `--xteink` / `--output xteink`. Output is
Markdown only (`…-newsletter-digest.xteink.md`); use the `epub` writer for a rich
offline ebook.
